import { ThemeProvider } from "../ThemeProvider";
import { About } from "../About";

export default function AboutExample() {
  return (
    <ThemeProvider>
      <About />
    </ThemeProvider>
  );
}
